server:
  port: 8080
  servlet:
    context-path: /sistema
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://10.0.0.40:5432/relacionamentos
    username: teste_relations
    password: merda
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
