spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/relacionamentos
    username: teste_relations
    password: merda
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
